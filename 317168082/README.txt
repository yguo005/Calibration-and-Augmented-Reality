camera_calibration.cpp:
- User collects frames by pressing 's', it will show how many frames are needed/collected
- User must collect at least 5 frames
- User presses 'c' to run calibration when ready
- Press 'v' to visualize current calibration

feature.cpp
Use '+' key: Increase threshold
Use '-' key: Decrease threshold
Press 'h' to switch between methods